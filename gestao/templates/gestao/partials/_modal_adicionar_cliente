{# gestao/partials/_modal_adicionar_cliente.html #}
<div class="modal fade" id="adicionarClienteModal" tabindex="-1" aria-labelledby="adicionarClienteModalLabel" aria-hidden="true">
    <div class="modal-dialog modal-lg modal-dialog-scrollable">
        <div class="modal-content">
            <form id="addClienteForm" method="post" action="{% url 'adicionar_cliente_modal' %}" novalidate>
                {% csrf_token %}
                <div class="modal-header">
                    <h1 class="modal-title fs-5" id="adicionarClienteModalLabel"><i class="bi bi-person-plus-fill me-2"></i>Adicionar Novo Cliente</h1>
                    <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
                </div>
                <div class="modal-body">
                    <div class="alert alert-danger d-none p-2 small" id="clienteFormErrors"></div>
                    <div class="row g-3">
                        <div class="col-12">
                            <label for="{{ form_cliente_modal.nome_completo.id_for_label }}" class="form-label">{{ form_cliente_modal.nome_completo.label }}</label>
                            {{ form_cliente_modal.nome_completo }}
                        </div>
                        <div class="col-md-6">
                            <label for="{{ form_cliente_modal.tipo_pessoa.id_for_label }}" class="form-label">{{ form_cliente_modal.tipo_pessoa.label }}</label>
                            {{ form_cliente_modal.tipo_pessoa }}
                        </div>
                        <div class="col-md-6">
                            <label for="{{ form_cliente_modal.cpf_cnpj.id_for_label }}" class="form-label">{{ form_cliente_modal.cpf_cnpj.label }}</label>
                            {{ form_cliente_modal.cpf_cnpj }}
                        </div>
                        <div class="col-md-6">
                            <label for="{{ form_cliente_modal.email.id_for_label }}" class="form-label">{{ form_cliente_modal.email.label }}</label>
                            {{ form_cliente_modal.email }}
                        </div>
                        <div class="col-md-6">
                            <label for="{{ form_cliente_modal.telefone_principal.id_for_label }}" class="form-label">{{ form_cliente_modal.telefone_principal.label }}</label>
                            {{ form_cliente_modal.telefone_principal }}
                        </div>
                    </div>
                </div>
                <div class="modal-footer">
                    <button type="button" class="btn btn-secondary" data-bs-dismiss="modal">Cancelar</button>
                    <button type="submit" class="btn btn-primary">Salvar Cliente</button>
                </div>
            </form>
        </div>
    </div>
</div>